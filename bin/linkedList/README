#LinkedList
- Node is the source class used to make new nodes for linked list.
- different methods like reversing a doubly linked list, sorting, merging two sorted linked list and inserting a new node are performed.
